apiVersion: v1
kind: Secret
metadata:
  name: {{ template "turtl.fullname" . }}
type: Opaque
data:
  {{- if .Values.turtl.setup_password }}
  setup_password: {{ .Values.turtl.setup_password | b64enc | quote }}
  {{ else }}
  setup_password: {{ randAlphaNum 10 | b64enc | quote }}
  {{- end }}
